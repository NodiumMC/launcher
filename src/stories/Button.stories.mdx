import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { Button } from '../components/atoms/Button'
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import { faThumbsUp } from '@fortawesome/free-solid-svg-icons'

<Meta title="Atoms/Button" component={Button} />

export const StoryButton = ({ text, ...args }) => <Button {...args}>{text}</Button>

# Button
<Canvas>
  <Story name="default" args={{ text: 'Кнопка' }}>
    {StoryButton.bind({})}
  </Story>
</Canvas>

# Button with icon
<Canvas>
  <Story name="with icon" args={{ text: <><FontAwesomeIcon icon={faThumbsUp}/>Кнопка</> }}>
    {StoryButton.bind({})}
  </Story>
</Canvas>

# Icon only
<Canvas>
  <Story name="icon only" args={{ text: <FontAwesomeIcon icon={faThumbsUp}/>, only: true }}>
    {StoryButton.bind({})}
  </Story>
</Canvas>

# Fetching
<Canvas>
  <Story name="fetching" args={{ text: 'Кнопка', fetching: true }}>
    {StoryButton.bind({})}
  </Story>
</Canvas>
