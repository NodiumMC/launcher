import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { VersionPicker } from '../components/molecules/VersionPicker'
import { useState } from 'react'
import { ProviderIcon } from '../core/providers/providers'

<Meta title="Molecules/VersionPicker" component={VersionPicker} />

export const providers = [
  { id: 'vanilla', label: ProviderIcon.vanilla },
  { id: 'fabric', label: ProviderIcon.fabric },
  { id: 'forge', label: ProviderIcon.forge },
  { id: 'quilt', label: ProviderIcon.quilt },
  { id: 'custom', label: ProviderIcon.custom },
]

export const versions = [
  {
    name: '1.19.3',
    id: '1.19.3',
    providers: ['vanilla'],
    isRelease: true,
    latest: true,
  },
  {
    name: '1.19.2',
    id: '1.19.2',
    providers: ['vanilla'],
    isRelease: true,
  },
  {
    name: '22w22a',
    id: '22w22a',
    providers: ['vanilla'],
    isRelease: false,
    isSnapshot: true,
  },
  {
    name: '1.19.1',
    id: '1.19.1',
    providers: ['vanilla'],
    isRelease: true,
  },
  {
    name: '22w10b',
    id: '22w10b',
    providers: ['vanilla'],
    isRelease: false,
    isSnapshot: true,
  },
]

export const StoryLargePicker = ({ ...args }) => {
  const [provider, setProvider] = useState('vanilla')
  const [version, setVersion] = useState('1.19.3')
  return <VersionPicker
    {...args}
    providers={providers}
    versions={versions}
    provider={provider}
    onProviderChange={setProvider}
    version={version}
    onVersionChange={setVersion}
    style={{ width: '100%' }}
  />
}

# VersionPicker
<Canvas>
  <Story name="default" args={{  }}>
    {StoryLargePicker.bind({})}
  </Story>
</Canvas>
